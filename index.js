function startGame() {
  Engine.init(); //Initializes the game
}

function restartGame() {
  Engine.restart();
}

function mainMenu() {
  window.location.reload(); //Reloads the window, sending user back to main menu
}